import React from 'react';
import './App.css';  // Assuming you have an App.css file for styling
import ToggleMessage from './ToggleMessage';  // Importing the ToggleMessage component

function App() {
  return (
    <div className="App">
      {/* Using the ToggleMessage component */}
      <ToggleMessage />
    </div>
  );
}

export default App;
